{
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.7",
    "prettier": "^2.6.1"
  },
  "lint-staged": {
    "**/*.{js,jsx,json,tsx}": [
      "prettier --write"
    ]
  },
  "scripts": {
    "lint-staged": "lint-staged",
    "prepare": "husky install"
  }
}
